for (let i = 0; i < 128; i++) {
    console.log(`unicode ${i}：`, String.fromCharCode(i));
    // const dom = document.createElement('span');
    // dom.innerHTML = String.fromCharCode(i);
    // document.body.append(dom);
}
